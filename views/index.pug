extends layout

block content
  if user
    //- div
    //-   a(href="/logout") ログアウト
    //-   p #{user.username}
    //-   p #{user.avatarUrl}
    div.my-3.p-3
      h6.border-bottom.pb-2 チャンネル一覧
      each channel in channels
        div.media.text-muted.pt-3
          a(href=`/channels/${channel.channelId}`) #{channel.channelName}
  else
    a(href="/login") ログイン
  

